"use strict";
var _ = require('lodash'),
    fs = require('fs'),
    path = require('path'),
    methods = require('./publicMethods/');

var method = function (data) {
    value = data;
    return value;
};

module.exports = method;