"use strict";

function method (to, data, from) {

    //Choose return depending on type of command
    /*
    return {
        type: "command",
        command: command,
        to: to,
        nick: nick,
        message: message
    }
    ||  return {
        type: "say",
        to: to,
        nick: nick,
        message: message
    }
    || return {
        type: "async"
    };
    */
}

//
var defaults = {
    description: {
        pl: ",komenda [data] - Bot robi [data]",
        en: ",command [data] - Bot does [data]"
    },
    aliases: [
        "command",
        "yourOwn"
    ]
};


module.exports = {
    method: method,
    defaults: defaults
};